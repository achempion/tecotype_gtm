# Agent notes
Tecotype's north star is equanimity: calm, clarity, and control; speed is expected, privacy is structural, and choices should be quiet, predictable, and honest.
Mailbox access is fully local: Tecotype infrastructure never receives user credentials or mailbox contents.
Read `product.md` and `positioning.md`; verify claims against `../tecotype` and never present planned work as shipped.
This workspace handles marketing and distribution only; assume the client works and product delivery is handled elsewhere.
For product, acquisition, and partnership research, assume published Tecotype will support macOS, Windows, and Linux and can build any feature, provider, service, or workflow integration that materially helps distribution. Do not reject an opportunity because that work is not implemented yet; mobile is out of scope. Still verify current implementation and each platform's release status before public claims.
Defer research and planning, merge duplicates, and never imply commitment. Count distribution only when a partner would introduce Tecotype with zero existing users through permanent pre-install discovery and an attributable first-action-to-paid funnel.
Keep new standalone documents at the root and experiment files under `experiments/`; do not otherwise reorganize until contexts are decided. See [DataForSEO setup](./tools/dataforseo.md).
